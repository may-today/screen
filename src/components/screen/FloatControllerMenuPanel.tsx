import Logo from '@/components/common/Logo'
import SongListSidebar from '@/components/controller/SongListSidebar'

export default () => {
  return (
    <div class="w-70vw max-w-300px">
      <Logo />
      <SongListSidebar />
    </div>
  )
}